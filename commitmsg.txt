fix(tv): consumir /api/ativos/<id>/dados e calcular lavagem interna (50h)

- TV agora lê dados reais do ativo via endpoint existente
- Exibe horas_motor como horas principais
- Calcula contagem regressiva de lavagem interna a cada 50h (arredondado)
- Mantém cotista e pendências como placeholder (—) por enquanto
