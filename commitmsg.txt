refactor: reescrita completa do módulo de dados do ativo
- adicionada lógica oficial de hora_embarcacao = horas_motor + horas_offset
- criado endpoint POST /api/ativos/<id>/ajustar-horas
- implementado tratamento seguro de offset
- resposta JSON agora inclui horas_offset e hora_embarcacao
- revisão total do cálculo do horímetro interno (horas_sistema_total)
- normalização da telemetria BrasilSat reorganizada
