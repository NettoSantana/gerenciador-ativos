feat: ajuste de horas da embarcação (offset) integrado ao painel

- adiciona botão 'Ajustar' no card Horas da Embarcação
- inclui modal completo para entrada do offset
- integra o modal com o endpoint POST /api/ativos/<id>/ajustar-horas
- atualiza automaticamente o painel após salvar o offset
- preserva toda a estrutura existente do painel
- painel.html totalmente reescrito mantendo compatibilidade
