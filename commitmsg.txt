fix: registra rotas de offset na API de ativos
- adiciona import do módulo offset em api_ativos/__init__.py
- garante que POST /api/ativos/<id>/offset seja registrado pelo Flask
- botão 'Ajustar' do painel passa a persistir horas_offset corretamente
