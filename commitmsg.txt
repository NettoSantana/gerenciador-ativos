feat(tv): painel TV sem base.html + consumo /api/dashboard-geral com auto-refresh

- Remove header/menu do base na tela da TV
- Busca dados via /api/dashboard-geral (no-store)
- Status de atualização + relógio
- Fullscreen com 1 clique (regra do browser)
