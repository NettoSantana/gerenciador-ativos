feat: rota de ajuste de horímetro
- adicionada POST /api/ativos/<id>/offset
- salva horas_offset no banco
- integra com cálculo existente em /dados
- corrige funcionamento do botão 'Ajustar' no Painel
